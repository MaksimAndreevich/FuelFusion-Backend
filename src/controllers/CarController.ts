import { CustomRequest } from "../../types/global";
import express from "express";

export const createCar = (req: CustomRequest, res: express.Response) => {};

export const editCar = (req: CustomRequest, res: express.Response) => {};

export const deleteCar = (req: CustomRequest, res: express.Response) => {};

export const getCar = (req: CustomRequest, res: express.Response) => {};

export const getAllCars = (req: CustomRequest, res: express.Response) => {};

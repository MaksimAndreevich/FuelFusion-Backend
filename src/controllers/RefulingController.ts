import { CustomRequest } from "../../types/global";
import express from "express";

export const createRefuling = (req: CustomRequest, res: express.Response) => {};

export const editRefuling = (req: CustomRequest, res: express.Response) => {};

export const deleteRefuling = (req: CustomRequest, res: express.Response) => {};

export const getRefuling = (req: CustomRequest, res: express.Response) => {};

export const getAllRefulingByCarId = (req: CustomRequest, res: express.Response) => {};

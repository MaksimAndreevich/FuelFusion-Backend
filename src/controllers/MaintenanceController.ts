import { CustomRequest } from "../../types/global";
import express from "express";

export const createMaintenance = (req: CustomRequest, res: express.Response) => {};

export const editMaintenance = (req: CustomRequest, res: express.Response) => {};

export const deleteMaintenance = (req: CustomRequest, res: express.Response) => {};

export const getMaintenance = (req: CustomRequest, res: express.Response) => {};

export const getAllMaintenancesByCarId = (req: CustomRequest, res: express.Response) => {};
